# 指数走势叠加图 — 改造需求

> 使用说明：把这份文档整份发给 AI，并附上当前图表组件的源码文件。
> 文档末尾的「待补充信息」两处需要你先填好再发。

---

## 一、现状与问题

当前图表用双纵轴，副标题标为"独立刻度"，实际效果是每条线各自归一化到自己的 min/max。

由此产生两个问题：

1. **陡峭度信息被抹掉**。每条线都被拉满画布高度，所以纳指、费半、标普看起来一样陡。费城半导体两年涨 124%、标普涨幅小得多，但图上看不出区别。
2. **VIX 破坏画面**。VIX 在 10~30 之间震荡、极端时冲到 60+，无论放在哪根轴上都会压扁其他线，或者自己被压成直线。

已有功能（**必须保留**）：
- 在时间窗口的起始点标注各指数的数值（VIX / VXFXI **不标注**）
- 最大回撤叠加层：曲线渐变着色 + 高点/谷底标记 + 回撤百分比标签
- 时间范围切换：1个月 / 3个月 / 6个月 / 今年 / 1年 / 2年 / 5年 / 10年
- 顶部四张数据卡片（当前点位 + 当日涨跌幅）
- 底部三张 VIX 阈值说明卡片

---

## 二、改造目标

单一默认展示模式，不显示任何纵坐标轴，四条线在同一张图中，**陡峭度可比**，VIX 不破坏画面和谐。

**不要拆成上下两个子图。**

---

## 三、需求明细

### 需求 1：三条股指共用一根隐藏对数轴，起点垂直错开

处理步骤：

1. 按当前时间窗口的**首个交易日**归一化，令首日值 = 100
2. 每条线再乘一个固定的错位系数，使三条线的起点垂直错开：

   | 指数 | 错位系数 | 视觉位置 |
   |---|---|---|
   | 标普500 | 1.0 | 最下 |
   | 纳斯达克100 | 1.35 | 中 |
   | 费城半导体 | 1.8 | 最上 |

3. 纵轴类型必须是 **对数刻度（log）**，`show: false`

**为什么必须是对数轴**：在对数轴上，整条序列乘以常数等于垂直平移固定像素，曲线形状和斜率完全不变——所以错位不会破坏陡峭度的可比性。换成线性轴，乘 1.8 会把波动幅度也放大 1.8 倍，费半会假性变陡，整个方案失效。**不要擅自改成线性轴。**

其他约束：
- 错位顺序**固定写死**（下→上：标普、纳指、费半），不要按涨幅动态排序，否则用户切换时间范围时线会跳位置
- 纵轴 `min` / `max` 必须显式写死，不要交给自动缩放
- 关掉一切"自动对齐/自动留白"的行为，否则显式写的 min/max 会被图表库悄悄改掉（ECharts 是 `alignTicks`，Chart.js 是 `grace` / `beginAtZero`，先确认用的是哪个库再对症处理）

### 需求 2：VIX 单独一根隐藏对数轴

- 纵轴类型：**对数刻度**，`show: false`
- 范围：`min: 10`，`max: 120`
- 用原始点位，**不做任何归一化**

**为什么是 10~120**：VIX 无上界（历史最高约 85），上限设得太低会被极端行情撑爆，设得太高则常态区间被压平。设 120 时各档位占画布高度：

| VIX | 占画布高度 |
|---|---|
| 15 | 16% |
| 20 | 28% |
| 30 | 44% |
| 60 | 72% |
| 85 | 86% |

常态区间 15~30 占据画布约 28% 的高度，起伏清晰可读；极端尖峰仍留有顶部空间。这个上限是"VIX 表现力 vs 画面和谐"的调节旋钮，需要更安静就调大到 200，需要更显眼就调小到 80。

**为什么用对数**：VIX 近似对数正态分布，从 10 到 20 和从 20 到 40 在市场含义上是等距的（波动率翻倍）。线性轴会把常态区间 10~30 压成一条平线——这正是当前要解决的问题之一。

视觉表现（**可读的背景层：起伏要看得清，但不能抢戏**）：
- 线宽 1.2px，透明度 0.65
- 面积填充透明度 0.12
- 颜色选低饱和度的中性色（灰蓝、灰绿一类），不要用高饱和的红/橙——它独占一根轴，不会和股指混淆，靠降饱和度来区分层次而不是靠降透明度
- 渲染层级在三条股指**下方**
- 在 VIX 轴的 **18** 和 **30** 位置画两条水平虚线（0.5px，透明度 0.4），与底部三张说明卡片的阈值一致

### 需求 3：起始点标注必须读原始数据 ⚠️

**这是最容易出错的地方。**

归一化和错位系数**只作用于绘图坐标**，不能影响任何显示给用户的数值。

- 起始点标注必须显示**该指数在该日期的真实点位**（如标普500 显示 `5,461.30`），**不是** 100、135 或 180
- VIX / VXFXI 在起始点**不标注**（保持现有行为）

实现上建议把原始数据和绘图数据分开存放，绘图用变换后的值，所有标注和 tooltip 一律回查原始数组。

### 需求 4：tooltip

显示真实数据，格式：

```
2025年4月29日
费城半导体   4,196.75    区间 -17.0%
纳斯达克100  18,240.15   区间  -8.2%
标普500      5,461.30    区间  -4.1%
VIX          24.17       温和波动
```

- 股指：原始点位 + **区间累计涨跌幅**（相对窗口首日，用原始数据计算）
- VIX：原始数值 + 档位文字（`< 18` 低波平稳 / `18–30` 温和波动 / `> 30` 情绪高压）

### 需求 5：删除模式切换按钮

- 删除 `涨跌幅%` 按钮
- 删除 `绝对数值` 按钮
- **保留** `最大回撤` 按钮（它是叠加层开关，不是显示模式）
- 保留全部时间范围按钮

真实数值由 tooltip、起始点标注和顶部四张卡片承担，删掉这两个按钮不会丢失信息。

### 需求 6：最大回撤叠加层

- 保留现有实现（渐变着色 + 高点/谷底标记 + 回撤标签）
- **只作用于三条股指**，VIX 不参与
- 计算必须基于**原始数据**，不受归一化和错位系数影响

（VIX 的"回撤"是市场转平静，是好事，和股指回撤语义相反，混在一起没有意义。）

### 需求 7：副标题文案

从 `左轴: 纳斯达克100 & 标普500 · 右轴: 费城半导体 & VIX (独立刻度)`

改为 `统一基准 = 区间首日 · 对数刻度 · 三线垂直错开 · VIX 为背景情绪层`

---

## 四、验收标准

改完后逐条自检：

1. 2年窗口下，费城半导体的曲线斜率**肉眼明显大于**纳斯达克100，纳斯达克100 明显大于标普500
2. 三条股指的起点垂直错开，顺序自下而上固定为标普、纳指、费半；切换任意时间范围后顺序不变
3. 起始点标注显示的是各指数当日**真实点位**（四位数或五位数），不是 100 / 135 / 180
4. VIX 在 15~30 常态区间内有**可见的起伏**，不是一条平直线
5. 模拟一段 VIX 冲到 60 的数据，检查它不超过画布高度 70%，且三条股指的视觉幅度**完全不变**
6. 切换到「1个月」窗口时，三条股指不会因为振幅太小而被压成三条水平直线（见下方备注）
7. 页面上不再有 `涨跌幅%` 和 `绝对数值` 按钮
8. 四条线全部开启时画面不拥挤，不需要靠点掉图例才能看清
9. 第九节回归清单里的每一项功能都实测通过
10. 拖拽平移在每一个时间范围下都正常，曲线不会在拖动过程中整体跳位置
11. 改造前后截图对比，除曲线的纵向位置和 VIX 的配色外，页面视觉**没有任何其他变化**

---

## 五、明确不要做的事

- 不要拆成上下两个子图
- 不要显示任何纵坐标轴的刻度或标签
- 不要把对数轴改成线性轴
- 不要给 VIX 计算最大回撤
- 不要修改顶部四张数据卡片和底部三张说明卡片
- 不要把归一化后的值显示在任何标注或 tooltip 里

---

## 六、备注：短窗口的潜在问题

错位系数 1.0 / 1.35 / 1.8 是按长窗口（1~2年）调的。切到「1个月」时股指振幅可能只有 ±3%，固定错位会让三条线之间空隙过大、曲线本身被压平。

如果验收标准第 6 条不通过，把错位系数改成跟随窗口内最大振幅动态计算：

```
振幅 = max(窗口内所有序列的 最大值/最小值)
错位步长 = 振幅 ^ 0.5
系数 = [1, 错位步长, 错位步长²]
```

这样长窗口错位大、短窗口错位小，各时间尺度下画面密度均匀。

**先按固定系数实现，验收第 6 条不通过再改动态。**

---

## 七、第一步：先清点现状

**改代码之前先做这件事，不要跳过。**

请先完整读一遍图表组件的源码，回答以下问题，**不要动任何代码**：

1. 图表库名和版本（ECharts / Chart.js / Recharts / D3 / 自研 canvas）
2. 该库的对数刻度怎么开启
3. 该库能否隐藏纵轴但保留缩放和交互
4. 现有的"起始点标注"和"最大回撤"叠加层是用什么机制实现的（原生 API / 插件 / 自定义绘制）
5. **列出这个组件当前支持的全部功能和交互**（叠加层、手势、参考线、标注、按钮、tooltip 行为等），逐条列清楚
6. 逐条判断：第 5 条里的每个功能，会不会被本次改造影响？受影响的说明原因和处理办法

第 5 条的清单请补充进第九节的回归测试清单。全部确认后给出改造方案，等我点头再动代码。

### 如果是 Chart.js

对应的 API：

- 对数刻度：`type: 'logarithmic'`
- 隐藏纵轴：`display: false`（或 `ticks.display: false` + `grid.display: false`）
- 两根纵轴：在 `scales` 里定义 `yPrice` 和 `yVix` 两个 key，数据集用 `yAxisID` 指定
- 范围：直接在 scale 上写 `min` / `max`
- 参考线（VIX 的 18 / 30）：需要 `chartjs-plugin-annotation` 插件
- 绘制层级：用数据集的 `order` 属性控制，**请实际验证方向**（Chart.js 的 order 语义容易记反），确保 VIX 在三条股指下方

Chart.js 的对数刻度有一个已知坑：即使写了 `min` / `max`，它仍可能按自己的算法生成刻度并微调边界。改完后务必用验收标准第 5 条实测一次。

---

## 九、功能保全清单

本次改造**只允许改变一件事：数据如何映射到纵坐标**。所有既有功能和交互必须原样保留。

改完后逐项实测：

- [ ] 最大回撤叠加层（渐变着色 + 高点/谷底标记 + 回撤百分比标签）
- [ ] 起始点数值标注（VIX / VXFXI 不标注）
- [ ] 图表拖拽平移 —— 支持任意时间范围下的全量历史手势拖拽
- [ ] 横向虚线参考线
- [ ] 时间范围切换按钮（8 个）
- [ ] 图例点击显示/隐藏单条曲线
- [ ] tooltip 十字准星与日期定位
- [ ] 顶部四张数据卡片
- [ ] 底部三张说明卡片
- [ ] _（第七节第 5 条清点出的其余功能，补充到这里）_

### 三个必须先决策的冲突点

改造后有两套坐标变换（归一化 + 错位系数），和拖拽、参考线存在语义冲突。**这三点必须先给出方案并等我确认，不要自行决定。**

**冲突 1：拖拽平移时，归一化基准要不要重算？**

推荐**不重算**。基准锁定在时间范围按钮对应的起始日；拖拽只是显示更多历史数据，早于基准日的部分自然落在 100 以下。这样起始点标注保持稳定，曲线不会在拖动时整体跳动。只有点击不同的时间范围按钮时才重算基准。

若选择重算，拖动过程中三条线会持续重新锚定，视觉上会非常跳。不推荐。

**冲突 2：基准日被拖出可视区后，起始点标注怎么处理？**

推荐让标注**跟随基准日一起移出视野**，语义清晰。不要把它钉在画布左边缘、变成"当前可见最左侧的值"——那样标注的含义会随手势漂移，用户无法判断它到底代表哪一天。

**冲突 3：横向虚线绑定哪根纵轴？**

现在只有一套坐标，改造后有两根轴。必须**显式指定**这条虚线绑 `yPrice` 还是 `yVix`，不能让图表库自己推断。如果它标记的是回撤基准位或价格参考位，应绑 `yPrice`；若是 VIX 阈值线，绑 `yVix`。

---

## 十、风格保全

页面视觉风格必须与现在**完全一致**：深色背景、现有配色、曲线发光效果、圆角胶囊标签、字体、按钮样式、卡片样式。

- 不要新增或修改任何全局 CSS
- 不要更换配色方案或主题变量
- 不要改动按钮、卡片、标签、图例的外观
- 不要引入新的 UI 组件库或图标库
- 不要"顺手优化"任何视觉细节

**唯一允许的视觉变化**是三处：VIX 曲线的配色（改为低饱和中性色）、VIX 的线宽与透明度、副标题文案。除此之外，改造前后的截图对比应该只有曲线的纵向位置不同。

---

## 十一、交付要求

- 分两次提交，不要一次全改完：
  - **第一次**：只做需求 1（三条股指共用对数轴 + 错位）和需求 3（起始点标注读原始数据）。做完停下来，我先看效果
  - **第二次**：需求 2、4、5、6、7
- 每处关键改动加一行注释说明改了什么
- 不要顺手重构无关代码
- 不要删除任何现有功能。如果某个功能与改造需求确实冲突、无法两全，**停下来问我**，不要自行取舍
